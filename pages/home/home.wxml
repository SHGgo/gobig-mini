<!--pages/home/home.wxml-->
<import src="/templates/index.wxml" />
<view class="container">
	<view class="search">
		<text class="iconfont icon-sousuo"></text>
		<input class="search-input"></input>
	</view>
	<view class="top-tab-swiper">
		<view class='tab  {{currentData == 0 ? "tab-border" : ""}}' data-current="0" bindtap='checkCurrent'>推荐</view>
		<view class='tab  {{currentData == 1 ? "tab-border" : ""}}' data-current="1" bindtap='checkCurrent'>排行榜</view>
	</view>
	<swiper current="{{currentData}}" class='swiper' style="height:600px;" duration="300" bindchange="swiperChange">
		<swiper-item>
			<view class='commend'>
        <template is="showBox"></template>
        <template is="showBox"></template>
        
      </view>
		</swiper-item>
		<swiper-item>
			<view class='swiper-current'>welcome come to 排行榜</view>
		</swiper-item>
	</swiper>
</view>